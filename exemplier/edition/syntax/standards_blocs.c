/* à la UNIX/C */                         /* à la Pascal */
void f(type p) {                          void f(type p)
    instructions;                         {
}                                            instructions;
                                          }

if (test) {                               if (test)
    instruction;                          {
}                                             instructions;
                                          }

if (test) {                               if (test)
  instructions;                           {
} else {                                      instructions;
  instructions;                           }
}                                         else
                                          {
                                              instructions;
                                          }

while (test) {                            while (test)
    instructions;                         {
}                                             instructions;
                                          }
										  

										  