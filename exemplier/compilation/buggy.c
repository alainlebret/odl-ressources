#include <stdio.h>

int main(void) {
    int index;

    for (index; index < 10; i++) {
        printf("%d * %d = %p\n", index, index, square());
    }
}

int square(int number) {
    return number * number;
}
